pre-commit:
    commands:
        run-hooks:
            run: ./run-hooks.sh {staged_files}
# commit-msg:
#     commands:
#         lint:
#             run: npx commitlint --edit
# pre-push:
#     commands:
#         test:
#             glob: '*.{js,ts,jsx,tsx}'
#             run: npm run test related -- --run {push_files}
